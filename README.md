# async pipe & thread control base class

## features

> * the pipe has same functions as  channel in golang, writed by c++, and thread safe
>
> * the thread control base class, can convert any child class into backend


